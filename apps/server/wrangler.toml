name = "fox-server"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# 무료 플랜에서는 SQLite 백엔드 Durable Object 만 생성/사용할 수 있다.
[[durable_objects.bindings]]
name = "ROOM"
class_name = "GameRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]

[vars]
# 배포 후 Netlify 도메인으로 바꾸세요. 비워두면 모든 오리진을 허용합니다.
ALLOWED_ORIGINS = "https://marvelous-rabanadas-6be02f.netlify.app"
