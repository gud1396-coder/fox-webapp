# 프로덕션 빌드에 박히는 서버 주소. 비밀이 아니라 공개 Worker 주소다.
# 개발 빌드(npm run dev:web)에는 적용되지 않는다 — 그때는 값이 없어 로컬 모드로 돈다.
# 실제 환경변수가 있으면 그쪽이 이긴다(Netlify 는 자기 설정값을 쓴다).
VITE_SERVER_URL=wss://fox-server.gud1396.workers.dev
