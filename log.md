# 작업 이력

무엇을 왜 고쳤는지의 기록이다.
**띄우고 배포하고 수업에 쓰는 방법은 [`progress.md`](progress.md) 에 있다.**

- 최종 갱신: 2026-09-01
- 상세는 `git show <해시>` 로 본다. 커밋 메시지에 원인·근거·검증값을 적어두었다.

---

## 이 프로젝트에서 배운 것 (다시 작업할 때 유의)

**1. 서버 도달성은 `curl` 결과만 믿지 말 것**
이 PC 의 git-bash `curl` 은 Windows schannel 백엔드를 써서(`libcurl/8.21.0 Schannel`)
Cloudflare Worker 와 TLS 핸드셰이크가 안 된다. 이것 때문에 "학교 망이 `workers.dev` 를
차단한다" 고 한동안 잘못 기록했다. 브라우저와 Node 는 멀쩡하다.

```
Chrome → 200 "ok" 223ms, WebSocket 개통 969ms
Node   → 200 "ok" 269ms
curl   → TLS 핸드셰이크 실패
프록시 → 없음 (ProxyEnable=0)
```

실제 사용자는 브라우저를 쓰므로 **브라우저 결과가 기준**이다.
`node -e "fetch('...').then(r=>r.text()).then(console.log)"` 로 교차 확인한다.

> **덧(2026-09-04): 이 PC 는 상태가 더 나빠졌다.** 지금은 `workers.dev` 로 가는
> **fetch 가 브라우저·Node 양쪽에서 모두 실패**한다(다른 도메인 fetch 는 80~290ms 로
> 정상). 반면 **주소창 접속과 WebSocket 은 멀쩡하다.**
>
> ```
> 브라우저 주소창 → /health "ok"          정상
> 브라우저 WebSocket → 서버 도달(503 수신)  정상
> 브라우저 fetch → workers.dev            전부 실패
> Node fetch   → workers.dev              전부 실패(타임아웃)
> 다른 도메인 fetch(cloudflare.com 등)     정상
> ```
>
> 그래서 **이 PC 에서는 관리자 기능(차단 on/off, 모든 방 초기화)이 동작하지 않는다** —
> 전부 fetch 를 쓴다. 게임(WebSocket)은 정상이다. 관리자 조작은 다른 망(휴대폰 데이터
> 등)에서 해야 한다. 도달성은 **전송 방식별로 따로** 확인할 것.

**2. 브라우저 자동화로 잰 값은 탭이 보이는 상태인지 확인할 것**
백그라운드 탭(`document.hidden === true`)에서는 `ResizeObserver` 가 발화하지 않는다.
화면 맞춤 배율이 재계산되지 않아 "레이아웃이 깨졌다" 고 오판할 뻔했다.

**3. 엔진을 고치면 Worker 재배포가 필수**
서버가 `packages/engine` 을 번들해서 쓴다. 프론트만 배포하면 규칙이 어긋난다.
`npm run deploy:server` 가 엔진 빌드까지 묶어서 한다.

**4. 규칙은 실물과 룰북 양쪽으로 확인할 것**
PDF 벡터 텍스트만으로는 아이콘으로 그려진 보너스 위치를 알 수 없었고,
초기값이 한 칸씩 밀려 있었다. 실물 점수판 이미지 확대 판독으로 확정했다.

---

## 1. 규칙 정확성

**`f03ab7a` 점수판 배치를 실물로 확정**
`sheet.ts` 의 `TODO()` 임시값 5곳을 실물 점수판(BGG pic3941962) 확대 판독으로 교체.
공식 영문 룰북(Schmidt 88198) 전문을 추출해 교차 검증했다.

- 초록·주황·보라의 임시값이 공통적으로 **한 칸씩 밀려** 있었다.
- 보라는 보너스가 5개인 줄 알았으나 실제로는 **11칸 중 9칸**에 있었다.
- 주황 `×3` 은 10번이 아니라 **11번(마지막) 칸**이었다.
- 라운드 트래커 5·6칸의 점 아이콘은 보너스가 아니라 **진행 인원 표시**였다.
- 여우는 시트 전체에 **5마리** (노랑 행4 / 파랑 행3 / 초록 7 / 주황 8 / 보라 7).

회귀 테스트 5개를 추가하고 `docs/RULES.md` 의 `[미확인]` 표기를 전부 걷었다.

**`9729044` 패시브의 은쟁반 특례 강제**
룰북: *은쟁반의 어떤 주사위도 쓸 수 없을 때만* 액티브의 주사위 칸에서 가져올 수 있고,
쓸 수 있는데 일부러 거르는 것은 금지다. 주석에 의도만 적혀 있고 구현이 없어서,
패시브가 언제든 액티브의 주사위를 가져갈 수 있었다.
`sheetOps.ts` 에 `canUseDie()` 를 추가해 `pickPlatter` · `skipPlatter` 양쪽을 막았다.
클라이언트도 같은 판정을 써서 못 고르는 주사위는 버튼으로 내보내지 않는다.

---

## 2. 접속·진행 버그

**`8c16151` 참가가 서버로 전달되지 않던 문제** — 가장 컸던 버그
방에 들어갔다가 **262ms 만에 로비로 튕겨나오는** 증상. 서버 방은 계속 비어 있었다.

원인이 두 겹이었다.
- `onJoin` 이 `setJoined(true)` 직후 50ms 뒤에 `send` 를 호출했는데, 그 `send` 는 클릭
  시점 렌더에서 만들어진 **로컬 모드용**이었다(스테일 클로저). join 이 브라우저 안
  엔진에만 적용돼 게임 화면이 잠깐 떴다가, 곧 도착한 서버의 최초 state 에 덮여 사라졌다.
- 설령 클로저가 최신이어도 소켓이 아직 안 열려서 `send` 가 액션을 **버렸다**.

참가를 다음 렌더의 이펙트로 옮기고 `useRoom` 에 전송 큐를 넣었다.

**`a0e58bc` 새로고침 재접속** — `join` 이 페이즈를 먼저 검사해 재접속이 막혔다. 순서를 뒤집었다.

**`962b143` 진행 중 합류** — 한 명이 먼저 시작하면 나머지가 전부 막혔다.
`gameOver` 만 거부하고, 합류자는 그 턴을 마친 것으로 두어 남의 진행을 막지 않게 했다.

**`6778866` 종료 화면이 `7/6` 으로 표시** — `round++` 를 하고 종료를 판정해서
없는 라운드가 표시됐다. 올리기 전에 판정하도록 바꿨다.

**`64240c1` 로비에 항상 "로컬 모드" 로 표시**
`useRoom(joined ? code : '')` 라 참가 전에는 코드가 빈 값이고 `online` 이 무조건
`false` 였다. 서버가 설정된 배포본에서도 **모든 접속자**가 "이 브라우저에서만
진행됩니다" 를 먼저 봤다. `HAS_SERVER` 로 판정을 분리하고, 학생에게 의미 없는
`VITE_SERVER_URL` 문구를 뺐다.

**방 코드 없이 참가하면 혼자 갇히던 문제** — 수업에서 실제로 터진 버그
증상이 둘로 보였지만 원인은 하나였다. ① 일부 학생만 대기자 목록이 비어 있고,
② 나갔다 다시 들어오면 "다른 사람을 기다리는 중" 에서 안 풀렸다.
둘 다 **방 코드가 빈 채로 참가된 것**이다.

- 참가 버튼이 **이름만** 검사했다. 코드가 비면 `code || 'LOCAL'` 로 `LOCAL` 이라는
  방에 넣었다.
- 그 렌더 시점엔 `code === ''` 이라 `useRoom` 이 로컬 모드였다. 참가가 브라우저
  엔진으로 들어가고 `joinSent` 가 잠긴다. 뒤늦게 `hashchange` 로 소켓이 열려도
  **참가를 다시 보내지 않아** 서버 방에는 끝내 못 들어갔다.
- 결과: 소켓은 열려 있는데 참가자 0명, 화면은 "온라인 모드". 기다려도 안 풀린다.

빠지는 경로가 둘이었다. 링크(`#/CODE`) 없이 맨 주소로 들어와 이름만 넣은 학생과,
`방 나가기` 가 `setCode('')` + 해시 삭제를 해서 코드 칸이 빈 상태로 다시 참가를
누른 학생. 링크로 들어온 학생만 정상이라 "몇 명은 되고 몇 명은 안 된다" 로 보였다.

고친 것 네 가지.
- 서버가 있는 빌드에서는 **코드 없이 참가 불가**(버튼 비활성 + 안내문). `'LOCAL'` 폴백 제거.
- `onJoin` 이 `setCode` 로 코드를 먼저 확정한다. `hashchange` 타이밍에 기대지 않는다.
- 참가를 `useRoom(code, hello)` 로 옮겨 **소켓이 열릴 때마다** 보낸다. `joinSent` 삭제.
  재접속·방 초기화 뒤에도 자동으로 방에 복귀한다(`join` 은 멱등).
- `방 나가기` 가 방 코드와 주소를 지우지 않는다.

로컬 서버로 재현·검증했다. 소켓을 강제로 끊으면 재접속하면서 참가가 자동
재전송되고 `connected` 가 false → true 로 돌아온다. 서버 주소가 없는 로컬 빌드는
전과 같이 코드 없이 참가된다.

> 진단하며 배운 것: **증상이 둘로 보여도 화면이 같으면 원인이 하나일 수 있다.**
> 소켓 미개통(0명) · 로컬 모드(1명) · 빈 방(정상 연결) 이 전부 똑같은
> "다른 사람을 기다리는 중" 으로 보였다. 로비가 `status` 를 안 받는 탓이었고,
> 바로 아래에서 고쳤다.

**로비가 연결 상태를 숨기던 문제 + 새로고침 자동 복귀** — 위 버그의 후속
위 버그를 고쳐도 "무엇이 잘못됐는지 화면이 말해주지 않는" 구조는 남아 있었다.
셋을 한꺼번에 고쳤다.

- **참가 전에도 방에 붙는다.** `useRoom(joined ? code : '')` → `useRoom(liveCode)`.
  로비에서 대기자와 연결 상태가 실시간으로 보이고, "방 X 에 이미 N명이 있습니다" /
  "아직 비어 있습니다. 코드가 맞는지 확인하세요" 로 방을 잘못 짚었는지 바로 안다.
  타이핑 중 방마다 붙지 않도록 600ms 잠잠해진 뒤 붙는다(`useSettled`).
  소켓이 이미 열린 채로 참가를 누르는 경우가 생기므로, `onopen` 말고도 `hello` 가
  뒤늦게 생겼을 때 보내는 경로를 `useRoom` 에 추가했다(같은 소켓 중복 전송은 막는다).
- **연결 상태 배너.** 끊김 → 빨간 배너 + "지금 다시 연결", 연결 중 / 방 합류 중은
  각각 다른 문구. 서버 방에 아직 못 들어갔으면 "다른 사람을 기다리는 중" 이 아니라
  "방에 들어가는 중" 이라고 말한다. 하단 문구도 `HAS_SERVER`(빌드 상수) 대신
  실제 `status` 를 쓴다.
- **새로고침 자동 복귀.** 참가한 방을 `fox.room` 에 적어두고, 이름이 있으면 버튼 없이
  돌아간다. 게임 중 새로고침은 200ms 안에 게임 화면으로 복귀한다(로비 깜빡임 없음).
  **나가기는 존중한다** — `방 나가기` 가 `fox.room` 을 지우므로 자동 복귀하지 않는다.
  링크로 *다른* 방에 온 경우도 자동 복귀하지 않고 로비에서 확인받는다.
- `useRoom` 에 `reconnect()` 추가. 백오프 타이머를 취소하고 즉시 다시 붙는다.
  15초 대기 중에 눌러도 0.5초 안에 재연결된다.

검증: 서버를 실제로 죽였다 살리며 배너 전환·자동 복구·재연결 버튼을 확인했고,
2인 게임 중 새로고침, 나가기 후 새로고침, 서버 없는 로컬 빌드까지 돌려봤다.

**한 명이 끊기면 반 전체가 멈추던 문제** — 엔진·서버 변경
`connected` 는 쓰기만 하고 규칙이 읽지 않았고, `webSocketClose` 는 비어 있어서
**끊김 자체가 감지되지 않았다.** 멈추는 자리가 세 군데였다.

- `afterRoundBonus` 는 전원의 보너스 해소를 기다린다 → 끊긴 사람이 막는다.
- `enterDice`/`active` 는 액티브만 조작할 수 있다 → 액티브가 끊기면 전원 정지.
- `afterPassive` 는 전원 `ready` 를 기다린다 → 끊긴 사람이 막는다.

손쓸 방법이 "모든 방 초기화"(전원 게임 날림)뿐이었다.

- 서버가 소켓에 `playerId` 를 붙여두고(`serializeAttachment`, 하이버네이션에도 남는다)
  `webSocketClose` 에서 `setConnected(false)` 를 넣고 방송한다.
  **같은 사람이 다른 탭으로 붙어 있으면 끊긴 것으로 보지 않는다.**
- `setConnected` 는 **클라이언트가 보내면 거부한다.** 안 막으면 남을 마음대로
  "끊긴 사람" 으로 만들어 턴을 건너뛸 수 있다.
- `skipDisconnected` 추가 — 연결된 사람 누구나 누른다. 끊긴 사람은 미처리
  보너스를 포기하고 이번 턴을 마친 것으로 처리된다. 액티브가 끊긴 경우는
  그 차례를 통째로 건너뛴다(은쟁반도 만들지 않는다).
- 게임 화면에 "OO 님의 연결이 끊겼습니다 · 건너뛰고 진행" 안내를 띄운다.
- 회귀 테스트 4개 추가(총 36개). 끊긴 액티브·끊긴 패시브가 실제로 막는 것을
  먼저 확인하고, 건너뛰기로 풀리는 것까지 검증한다.

**하트비트와 유령 소켓** — 위를 만들다 발견
서버에 `ping`/`pong` 이 있는데 클라이언트가 한 번도 쓰지 않았다. 모바일·절전 망에서
소켓이 조용히 죽으면 `onclose` 가 안 떠서 재접속도 안 걸린다. 25초 ping / 10초 안에
pong 이 없으면 직접 끊도록 했다. 화면 복귀·네트워크 복구 시에는 백오프를 기다리지
않고 바로 붙는다(`visibilitychange`, `online`).

하트비트를 재다가 **핑이 두 벌 나가는 것**을 발견했다. `alive` 를 컴포넌트 공유
ref 로 쓴 탓이다. 아직 `CONNECTING` 인 소켓을 정리하며 닫으면, 그 소켓의 뒤늦은
`onclose` 가 (그 사이 되살아난 `alive` 를 보고) 재접속을 하나 더 만든다. 버려진
소켓이 열린 채 남아 하트비트가 두 벌 돌고, **서버는 그 사람이 아직 붙어 있다고 여겨
끊김을 감지하지 못한다** — 위에서 만든 기능을 그대로 무력화한다.
이펙트 인스턴스 전용 `stopped`/`current` 로 바꿔 소유권을 분명히 했다.

> 배운 것: **하이버네이션 소켓에서 "누가 끊겼나" 를 알려면 소켓에 신원을 붙여둬야
> 한다.** 그리고 재연결 로직의 "아직 유효한 시도인가" 판정은 **공유 ref 가 아니라
> 시도별 지역 상태**여야 한다. 공유 ref 는 정리와 재시작이 겹칠 때 반드시 샌다.
>
> 측정할 때 `document.visibilityState` 를 먼저 볼 것(log.md 앞쪽 교훈 2번).
> 숨은 탭에서는 타이머가 병합되어 하트비트 간격이 엉뚱하게 읽힌다. 실제로 이 때문에
> "인터벌 3개" 로 잘못 읽을 뻔했고, 주기를 2초로 줄여 다시 재고서야 정확히 2개임을
> 알았다.

**로그 길이 제한** — 상태는 액션마다 통째로 저장·방송된다. 접속이 불안정한 사람이
있으면 끊김 기록이 계속 쌓이므로 `reduce` 에서 최근 200줄로 자른다.


**관리자 패널이 차단 상태를 거짓으로 표시하던 문제**
서버가 실제로는 차단(503)인데 패널에는 "허용됨" 으로 떴다. 원인은 두 줄이다.

```js
const [locked, setLocked] = useState(false);              // 기본값이 "허용됨"
fetch(httpBase + '/admin/lock').catch(() => undefined);   // 조회 실패를 조용히 삼킴
```

조회가 실패하면 기본값이 그대로 남아 **차단 중인데 허용됨으로 보인다.** 게다가
버튼이 `locked: !locked` 를 보내므로, 그 상태에서 누르면 오히려 차단을 거는 셈이라
영영 풀지 못한다.

- 상태를 `boolean | null` 로 바꿔 **"확인 못 함"** 을 따로 표시한다. 실패를 삼키지 않는다.
- 버튼을 "연결 허용하기" / "연결 차단하기" **두 개로 분리**했다. 지금 상태를 뒤집는 게
  아니라 **원하는 상태를 직접 지정**하므로, 표시가 틀렸거나 못 읽었어도 안전하다.
- "다시 확인" 버튼 추가. 요청 실패 시 원인을 알려준다(다른 망에서 시도해 보라는 안내).

**연결 진단이 거짓 음성을 내던 문제**
`fetch('/health')` 하나만 봤는데, **기기에 따라 fetch 만 막히고 WebSocket 은 멀쩡한
경우가 있다.** 실제로 이 PC 가 그렇다 — workers.dev 로 가는 fetch 는 전부 실패하는데
(브라우저·Node 둘 다), 브라우저 주소창 접속과 WebSocket 은 정상이다. 그래서 게임은
되는데 진단은 "연결 안 됨" 이 떴다.

게임이 실제로 쓰는 **WebSocket 을 먼저** 시험하도록 바꿨다. 실패했을 때만 `/health`
를 보조로 보고, 서버는 살아 있는데 방 접속만 거부되면 **"관리자 차단을 확인하라"** 고
안내한다. 이 경우에는 "학교 망 차단" 힌트를 띄우지 않는다 — 엉뚱한 곳을 찾게 된다.

> 배운 것: **진단 도구는 실제로 쓰는 전송 방식으로 시험해야 한다.** `/health` 를
> fetch 로 찔러본 결과는 게임(WebSocket)의 가부와 다를 수 있다. 그리고 **상태 조회에
> 실패했을 때 "정상" 을 기본값으로 두면 안 된다** — 모르는 것은 모른다고 해야 한다.

---

## 3. UI

**`60cf278`** 실물 점수판 배치 반영(노랑|파랑 위, 초록·주황·보라 아래), 컨트롤바 분리,
라운드 트랙과 액션 바(각 7칸) 추가, 실시간 점수표 탭, 규칙 툴팁, 방 나가기.
**`b5c0b07`** 설명서를 중앙 모달로, 보너스를 `X초`·`주5` 대신 **색 배지**로.
**`edc398d` `0aeb6b5` `4ecccd2` `76b4d54`** 칸 경계·크기·라운드 트랙 비율 조정.
**`d8d9f76`** 노랑 → 빨강, 화면 맞춤 자동 배율(0.4~1.8배, `zoom`).
**`e145548`** 여백 정리 — `1fr 1fr` 그리드가 상자를 억지로 늘리던 것을 내용 크기로.
시트 폭 1198 → 869px.
**`97da4bd`** 수권 점수표를 격자 오른쪽 세로 2열로. 869 → 659px. 상자 높이도 맞췄다.
**`fb9c532`** 좌우 2단 컨트롤바 + 플레이어별 탭.
1366×768 실측에서 왼쪽 바가 779/712 로 넘쳤던 것을 좌 190px(이동) / 우 300px(주사위)
로 나눠 **양쪽 712/712** 로 맞췄다.
**`6778866`** 은쟁반을 오른쪽 바 최상단으로(Netlify 배지와 겹쳐 가려졌다),
관측 주기를 세로 목록으로, **눈 다시 입력** 버튼 추가.

### 실물 주사위에 맞춘 표기
흰색 → **핑크**, 노랑 → **빨강**. 엔진 식별자(`white`, `yellow`)는 그대로다.

---

## 4. 운영 기능

**`467968e` 관리자 모드** — 테마 선택, 브라우저 정보 삭제, 모든 방 초기화.
방을 전부 지우려면 어떤 방이 열려 있는지 알아야 하는데 Durable Object 는 목록 조회가
안 된다. `__index__` 인스턴스에 방 코드를 모아두고(접속 때 `waitUntil` 로 기록해
응답 지연 없음) 초기화 시 전부 순회한다.
비밀번호는 공개 저장소에 남지 않도록 **Worker 시크릿**으로 넣었다.

**`6778866` 서버 연결 차단 on/off** — 수업 시간 외 사용 제한용.
검증: 차단 시 `503`, 해제 시 `101` 로 복구.

**`3e93974` 로비를 대기실로** — 실시간 참가자 목록, 혼자면 시작 잠금.

**`76f9211` 연결 진단 버튼** — 기기별 서버 도달성 확인.
(workers.dev 차단 오진 때 만들었지만, 접속 문제 분류에 계속 쓸모가 있어 남겼다.)

**`5fe4c83` Worker 자동 배포 제거**
`CLOUDFLARE_API_TOKEN` 이 유효하지 않아(`code 9106`) `deploy-server` 워크플로가 푸시마다
실패했다. 토큰 재발급 대신 워크플로를 걷어내고 `npm run deploy:server` 로 대체했다.
`ci.yml`(테스트·빌드 검사)은 그대로 둔다.

되살리려면:
```bash
git show e0cedb7:.github/workflows/deploy-server.yml > .github/workflows/deploy-server.yml
```
토큰은 https://dash.cloudflare.com/profile/api-tokens 의 **Edit Cloudflare Workers**
템플릿으로 발급하고, 등록 전에 반드시 확인한다.
```bash
curl -H "Authorization: Bearer <토큰>" https://api.cloudflare.com/client/v4/user/tokens/verify
```

---

## 5. 정정된 기록

**`aa5234f` workers.dev 차단은 오진이었다**
`curl` 결과만 보고 "학교 망이 `workers.dev` 를 SNI 단위로 차단한다" 고 판단해
커스텀 도메인 연결이나 망 관리자 요청을 검토했으나, **문제 자체가 없었다.**
원인은 위 "배운 것" 1번의 schannel 이슈다. 대응은 필요 없었다.

---

## 6. 배포 이력

| 대상 | 방법 |
|---|---|
| GitHub | `cd1c866`(원격 초기 커밋) 위로 rebase 후 첫 푸시 |
| Netlify | `main` 푸시마다 자동. `VITE_SERVER_URL` 등록 후 캐시 없이 재배포 |
| Netlify 공개 전환 | Private → **Public** (기본이 Private 이라 학생이 못 들어왔다) |
| Cloudflare Worker | CLI 로 여러 차례. 이후 `npm run deploy:server` 사용 |

Netlify UI 가 `Sites` → **`Projects`** 로 바뀌어 있어 설정 경로를 찾기 어려웠다.
환경변수는 **Project configuration → Environment variables**,
공개 전환은 **Project configuration → General → Visitor access** 에 있다.
